ADcheckLoginStatus()

Description
    bool ADcheckLoginStatus(void)
    Checks a user's authType and whether or not sessionGet("username)" is set.

Return Values
    Returns a boolean value depending on the status of the function.  Returns FALSE if the authType is not "ldap" or if sessionGet("username") returns 
    FALSE.  Otherwise exists gracefully.

Examples
    <?php
	...
	function accessControl_ad_user($value,$state=FALSE) {
	    $returnValue = ADcheckLoginStatus();
	    if($returnValue === FALSE) {
		return(FALSE);
	    }
	...
	}
	...
    ?>
    
See Also
    accessControl_ad_user()
    accessControl_ad_auth()
    accessControl_ad_group()
    accessControl_ad_ou()
