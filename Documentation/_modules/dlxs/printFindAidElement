dlxs::printFindAidElement()

Description
    public string dlxs::printFindAidElement([array $attPairs = NULL])
    This function returns a user readable value.

Parameters
    attPairs
    	An array that must have a "name" key and an optional boolean "tag" key.

Return Values
	A string containing the value of the element.
    
Examples
    {dlxs var="faElement" name="//FullTextResults/Results/DocContent/RegionContent/ead/frontmatter/titlepage/publisher"}

Notes
   This function is called from the dlxsDisplayTemplate function by using the {dlxs var="faElement" ... } tag.

See Also
	dlxsDisplayTemplate()
	dlxs::printFindAidSubjects()
