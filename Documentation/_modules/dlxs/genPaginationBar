dlxs::genPaginationBar()

Description
    public string dlxs::genPaginationBar(object $xml, string $file)
    Generates a list of links to skip through pages. Sample output could look as follors: "1 21 41 61 81 101 next >>"

Parameters
    xml
    	An xml file containing the values of each of the elements contained in the template file.
    file
    	A template file containing markup.

Return Values
    A string in the format of the template file with all of the values placed into it.
    
Examples
    <?
    ...
    print $dlxs->genPaginationBar($xml,"/path/to/template.php");
    ...
    ?>

Notes
	This function makes use of the dlxs::buildTemplate() function, therefore looping is possible if desired.
   
See Also
	dlxs::buildTemplate()
