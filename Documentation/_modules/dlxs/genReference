dlxs::genReference()

Description
    private string dlxs::genReference(void)
    Generates the appropriate string correspdonding to the view type indicated in $this->viewType.
	
Parameters
	None.
	
Return Values
	Returns a string, $ref, which corresponds to $this->viewType.
	There are three possibilities:
	1. "records" sets $ref to "ref=records";
	2. "fullrecord" sets $ref to "ref=fullrecord";
	3. and default sets $ref to "".

Examples

Notes

See Also
