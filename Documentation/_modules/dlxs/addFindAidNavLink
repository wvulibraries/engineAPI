dlxs::addFindAidNavLink()

Description
    public bool dlxs::addFindAidNavLink(string $id, string $title)
	Adds a link and its associated information, $id and $title, to the array $this->findAidNavLinks.

Parameters
    id
		The Div ID that the information will be in.
    title
		The link's display text.
    
Return Values
    Returns TRUE when successful.  Returns FALSE otherwise.

Examples
	<?
	...
	$dlxs->addFindAidNavLink("titlePage","Title Page");
	...
	?>

Notes

See Also
    