dlxs::dlxsDisplayTemplate()

Description
    string dlxs::dlxsDisplayTemplate(array $matches)
    
Parameters
    matches
		An array of matches returned from the regular expression "/\{dlxs\s+(.+?)\}/".
		
Return Values
	A string containing the output of the function called inside the var match.
    
Examples
	<div id="titlePage" class="toggle">
		<h2>Title Page</h2>
		<p>{dlxs var="faElement" name="//FullTextResults/Results/DocContent/RegionContent/ead/frontmatter/titlepage/publisher"}</p>
		<p>{dlxs var="faElement" name="//FullTextResults/Results/DocContent/RegionContent/ead/frontmatter/titlepage/titleproper"}</p>
		<p>{dlxs var="faElement" name="//FullTextResults/Results/DocContent/RegionContent/ead/frontmatter/titlepage/author"}</p>
	</div>

Notes
	When a {dlxs ... } tag is found, engineCMS performs a regular expression match against it and calls this function. A var is required in the form of {dlxs var="" ... }.
	
	var must contain one of the following:
	1. collTitle
	2. collGroup
	3. contactLink
	4. contactText
	5. pageBar
	6. sliceBar
	7. findAidLeftNav
	8. faElement
	9. faElementSubjects
   
See Also
    dlxs::getDefaultTitle()
	dlxs::getDefaultGroup()
    dlxs::getDefaultContactLink()
    dlxs::getDefaultContactText()
    dlxs::pageBar()
    dlxs::sliceBar()
    dlxs::genFindAidLeftNav()
    dlxs::printFindAidElement()
    dlxs::printFindAidSubjects()
