pagination::__construct()

Description
    pagination::__construct(int $total, object $engine)
    The constructor for pagination.  Initially sets up a few variables, $this->cleanGet = $engine->cleanGet and $this->totalItems = $total.
Parameters
    total
        The total number of pages which should appear in the pagination.
    engine
	The engine object.
