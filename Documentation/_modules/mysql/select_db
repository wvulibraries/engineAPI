engineDB::select_db()

Description
    public bool engineDB::select_db(string $db)
    This is a wrapper function for mysql_select_db().

Parameters
    db
        The name of the database to be selected.

Return Values
    Returns FALSE if $db is not set or there is a failure otherwise.  Returns TRUE on success.
    
Examples
    <?php
	...
	if($this->select_db($this->database)) {
	    $this->status = TRUE;
	    return(TRUE);
	}
	...
    ?>
