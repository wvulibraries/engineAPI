mailSender::sendEmailBulk()

Description
    public mixed mailSender::sendEmailBulk(string $sendID, array $emails, array $dbInfo)
	Populate table with e-mails.

Parameters
    sendID
		The send ID associated with an e-mail message.
    emails
		An array of e-mail messages.
    dbInfo
		An array containing database information.

Return Values
    Returns a string, $result, upon completion.  The string, $result, originates from PHP's exec() function which executes when sendEmailBulk() calls 
	performBulkSend().  If, however, the MySQL query is unsuccessful, the function returns webHelper_errorMsg("Failed to populate bulk email table.").

See Also
	mailSender::performBulkSend()