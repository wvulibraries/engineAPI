mailSender::addSubject()

Description
    public void mailSender::addSubject(string $subject)
	Sets $this->subject to the parameter, $subject.

Parameters
    subject
		String value containing the e-mail's subject.

See Also
	mailSender::addAttachment()
    mailSender::addBCC()
    mailSender::addBody()
    mailSender::addCC()
    mailSender::addRecipient()
    mailSender::addReplyTo()
	mailSender::addSender()