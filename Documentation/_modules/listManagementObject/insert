listManagement::insert()

Description
    public string listManagement::insert()
    Insert data into the database, after performing several checks on the data.  Output success or error message when appropriate.

Return Values
    String with HTML markup, containing either success or failure message.

Examples
    <?php
	...
    	//Add new administrator
	if(isset($engine->cleanPost['MYSQL']['configValidIP_submit'])) {
	    require_once($engineVars['baseRoot']."/pageIncludes/admin/includes/adminLog.php");
	
	    $logData = array();
	    $logData['action'] = "ipaccess: Insert IP Access into List";
	    $logData['insertDisplayName'] = $engine->cleanPost['RAW']['ip_insert'];
	
	    $logResult = adminLog($logData);
	
	    if ($logResult === TRUE) {
		$errorMsg .= $listObj->insert();
	    }
	    else {
		$errorMsg .= webhelper_errorMsg("Admin Log Failure.");
	    }
	}
	...
    ?>
