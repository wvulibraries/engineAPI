listManagement::buildUpdateString()

Description
    private string listManagement::buildUpdateString(string $row)
    Creates a valid MySQL update string.

Parameters
    row
        Valid MySQL row name.

Return Values
    A string containing the concatenation(s) of $this->engine->openDB->escape($I["field"]) and $this->engine->cleanPost['MYSQL'][$I["field"]."_".$row], comma delimited.

See Also
    listManagement::buildInsertUpdateString()
