listManagement::buildFieldListInsert()

Description
    private string listManagement::buildFieldListInsert()
    Builds a string, $output, from the fields contained in $this->fields and $this->hiddenFields which are not flagged as disabled or of plainText or radio 
    type.

Return Values
    Returns a string containing the elements of $this->fields, comma delimited.

Examples
    <?php
	...
	if ($this->updateInsert === FALSE) {
	    $sql = sprintf("INSERT INTO %s (%s) VALUES(%s)",
		$this->engine->openDB->escape($this->table),
		$this->buildFieldListInsert(),
		$this->buildFieldValueInsert(),
	    );
	}
	...
    ?>

See Also
    listManagement::buildFieldValueInsert()
