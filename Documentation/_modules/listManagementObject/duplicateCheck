listManagement::duplicateCheck()

Description
    private bool listManagement::duplicateCheck(int $new, string $col)
    Checks DB for an existing entry.

Parameters
    new
	An integer concatenated to the column name.
    col
	Existing DB column name.

Return Values
    Returns boolean TRUE or FALSE.  Returns TRUE if matching column data already exists in the DB.  Otherwise, returns FALSE.
