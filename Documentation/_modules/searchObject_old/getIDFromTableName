search::getIDFromTableName()

Description
    public string search::getIDFromTableName(string $tableName)
    Obtain a table's ID by providing a table's name.

Parameters
    tableName
	A string, which contains the table's name to compare.

Return Values
    String variable specifying the table ID.

Examples
    <?php
	...
	foreach ($this->tableLinks as $table) {
	    if (strcmp($this->tables[$tableID]['table'],$table['table1']) == 0) {
		$ID = $this->getIDFromTableName($table['table2']);
		$this->addToSearchType($searchType,$ID);
	    }
	}
	...
    ?>
