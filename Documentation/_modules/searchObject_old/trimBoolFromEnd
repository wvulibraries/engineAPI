search::trimBoolFromEnd()

Description
    private string search::trimBoolFromEnd(string $keywords)

Parameters
    keywords
	A string variable to be evaluated.

Return Values
    Returns original string, stripped of boolean operands.

Examples
    <?php
	...
	$output = array();
	// convert string from text field into array
	$tmpArray = $this->searchStringToArray($searchString[$i]);
	foreach ($tmpArray as $key => $value) {
	    $output[$type][] = $value;
	}
	$output[$type] = $this->trimBoolFromEnd($output[$type]);
	...
    ?>
