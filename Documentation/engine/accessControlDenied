engineCMS::accessControlDenied()

Description
    private void engineCMS::accessControlDenied(void)
    Erases the output buffer and calls header() to redirect user to the login page.

Parameters
    
Return Values
    
Examples

    <?php
	...
	if($action == "denyAll") {
	    $this->accessControlDenied();
	    exit;
	}
	...
    ?>

Notes
   
See Also
    engineCMS::accessControl()
