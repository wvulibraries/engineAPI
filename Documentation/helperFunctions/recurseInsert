recurseInsert()

Description
    mixed recurseInsert(string $file [, string $type = "php" [, object $engine = NULL]])
    
Parameters
    file
	type
	
	engine
		The engine object.  NULL by default.
	
Return Values
    
Examples
	<?php
		...
		switch($attPairs['name']) {
			case "include":
			    $output = recurseInsert($attPairs['file'],$attPairs['type'],$this);
				break;
			case "date":
			    $output = tempDate($attPairs);
				break;
		...
	?>