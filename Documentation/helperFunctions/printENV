printENV()

Description
    NULL printENV([object $engine = NULL])
	Prints Engine and local variables.
	
Parameters
    The engine object.  NULL by default.

Return Values
    Returns NULL upon completion.

Notes
	Engine variables are those stored in $engineVars.  $localVars are obtained from the statement $localVars = $engine->localVarsExport().