getGroups()

Description
    array getGroups(array $memberof)
    Iterates through the array, $memberof, pushing each array row onto $groups.

Parameters
    memberof
		An array containing group information.

Return Values
    Returns an array containing a list of AD groups.

See Also
    getGroup()