getGroup()

Description
    mixed getGroup(string $dn)
    Searches the parameter, $dn, for text matching a regular expression pattern, /^CN=(.+?)\,/

Parameters
    dn
		The input string to search.

Return Values
    Returns $matches[1] if it is set.  Otherwise returns FALSE.

Examples
    <?php
	...
	$groups = array();
	    for($I=0;$I<$memberOf["count"];$I++) {
		$group = getGroup($memberOf[$I]);
		array_push($groups,$group);
	    }
	...
    ?>

See Also
    getGroups()
