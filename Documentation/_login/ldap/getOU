getOU()

Description
    mixed getOU(string $dn)
    Searches the parameter, $dn, for text matching a regular expression pattern, /^.+OU=(.+?)\,.+$/

Parameters
    dn
		The input string to search.

Return Values
    Returns $matches[1] if it is set.  Otherwise returns FALSE.

Examples
    <?php
	...
	$_SESSION['ou'] = (isset($entry[0]["dn"]))?getOU($entry[0]["dn"]):FALSE;
	...
    ?>

See Also
    ldapLogin()
    getGroup()